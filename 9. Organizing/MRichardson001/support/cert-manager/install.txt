STEP 1

Add cluster issuer.

STEP 2

helm install --name cert-manager --namespace cert-manager --version v0.10.0 jetstack/cert-manager --set nodeSelector."beta\.kubernetes\.io/os"=linux --set ingressShim.defaultIssuerName=letsencrypt-prod --set ingressShim.defaultIssuerKind=ClusterIssuer


STEP 3

Add ingress controller
